[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[dev]
  command = "npm run serve"
  targetPort = 8080
  framework = "#custom"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
